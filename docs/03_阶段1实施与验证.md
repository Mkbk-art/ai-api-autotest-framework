# 阶段 1 实施与验证报告

## 1. 实施范围

本次完成项目计划书阶段 1 的 P0 修复，并完成阶段 2 的最小 Mock 验证。由于当前
没有短链接 SaaS 仓库、接口文档和启动环境，本次没有越界编造真实业务接入、
数据库校验或 AI 能力。

## 2. 隔离工作区

沙箱无法直接解析 GitHub 域名，因此无法执行普通 `git clone`。本次依据联网审查
获得的上游文件，在独立 Git 仓库中重建阶段 1 所需基线，并记录上游 commit 和
MIT License。该目录不是上游仓库的字节级镜像，事实已写入 `BASELINE_SOURCE.md`。

分支：`phase1/p0-fixes`

提交：

```text
67b4f53 chore: reconstruct reviewed baseline for stage 1
d8aead5 fix: repair yaml loading and request headers
9828553 feat: add reproducible runner and independent mock flows
```

## 3. 已实现并验证

| 能力 | 状态 | 证据 |
|---|---|---|
| YAML 严格加载和路径修复 | 已验证 | 7 项 YAML/path 单元测试 |
| JSON 参数重复传递修复 | 已验证 | JSON 转发测试 |
| form/params/files 转发 | 已验证 | 3 项参数类型测试 |
| Header 覆盖和删除 | 已验证 | 单元测试 + core 无 Token 场景 |
| 敏感 Header/字段脱敏 | 已验证 | 递归脱敏单元测试和执行日志 |
| 配置优先级 | 已验证 | CLI/环境变量/命名文件/默认值测试 |
| timeout 与 TLS 配置传递 | 已验证 | RequestBase 单元测试 |
| Marker 独立执行 | 已验证 | 三个独立子进程回归测试 |
| 受控 Mock API | 已验证 | 登录—发布—调用集成测试 |
| 0 用例失败 | 已验证 | Pytest 退出码 5 + run.json |
| JUnit XML | 已验证 | 三个层级均生成 XML |
| Allure 缺失降级 | 已验证 | 没有插件时不传未知参数 |
| Allure HTML | 未验证 | 沙箱缺少 allure-pytest 和 Allure CLI |

## 4. 最新验证结果

```text
用例收集：6 tests collected
smoke：2 passed, 4 deselected
core：2 passed, 4 deselected
regression：2 passed, 4 deselected
框架测试：31 passed
代码覆盖率：61%
compileall：exit 0
```

覆盖率是当前阶段的基线，不代表最终目标。较低模块主要是尚未完成的基线断言、
上下文和请求编排分支，后续阶段会随着功能重构补齐。

## 5. 输出文件

- `docs/evidence/01_collect_only.txt`
- `docs/evidence/02_smoke.txt`
- `docs/evidence/02_core.txt`
- `docs/evidence/02_regression.txt`
- `docs/evidence/03_framework_tests_and_coverage.txt`
- `docs/evidence/04_compileall.txt`
- `reports/runs/stage1-*/junit.xml`
- `reports/runs/stage1-*/run.json`
- `reports/coverage.xml`

## 6. 当前结论

阶段 1 的代码 P0 修复和 JUnit/Mock 验收已完成。Allure HTML 因环境依赖缺失仍需
在本地或 CI 安装完整依赖后补验，所以不能声称“完整 Allure 报告已验证”。

下一步进入阶段 2：将文件型 `extract.yaml` 上下文迁移为隔离的内存上下文，完善
断言引擎、异常响应测试、超时/连接失败测试和完整 Allure 附件验证。
