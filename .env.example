# 可选的通用 OS 环境变量覆盖示例。
# 项目自身账号、域名、MySQL/Redis 等本地配置优先放在对应 env.<project>.yaml 中。
# OS 环境变量主要保留给 CI Secret、临时覆盖或历史 DSL 兼容。

# API_HOST=http://127.0.0.1:8000
# API_TIMEOUT=10
# API_VERIFY_SSL=false
# API_USE_MOCK=false
# LOG_LEVEL=INFO
